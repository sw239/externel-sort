We have to run the MakeRuns first with a integer that set the size of heap.
The PolyMerge will take 2 integer, first one is how many files to use for a polyphase sort merge, the other one should be the same as Runs from MakeRuns tells. The process takes pretty long.
